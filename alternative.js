//Alternative export ways:

//We can set the object's property equal to the value we want. In this case is an array:
module.exports.items = ['item 1', 'item 2']


//Other way to export:

const person = {
    name: 'Vicky'
}

//Then we export the object the property 'singlePerson' equal to 'person' object
module.exports.singlePerson = person